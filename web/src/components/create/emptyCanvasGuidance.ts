/**
 * The empty result canvas's wording while the recipe REQUIRES a prompt. The
 * optional and prompt-ignored sentences are studio's; this is the one surface-
 * specific case, and `promptGuidance` picks between the three exactly once.
 */
export const REQUIRED_PROMPT_GUIDANCE =
  "Describe an image below, pick a look, and press Generate. Everything runs on your own machine.";
