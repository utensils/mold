/**
 * The one plain sentence about a machine now lives in
 * `@studio/lib/machineSentence` so web says it too. Re-exported here so every
 * existing desktop import keeps working.
 */
export { machineHardware, machineLocation, machineSentence } from "@studio/lib/machineSentence";
export type { MachineIdentity } from "@studio/lib/machineSentence";
