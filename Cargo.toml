[workspace]
members = [
    "crates/mold-core",
    "crates/mold-db",
    "crates/mold-inference",
    "crates/mold-server",
    "crates/mold-cli",
    "crates/mold-discord",
    "crates/mold-tui",
    "crates/mold-catalog",
    "crates/mold-scheduler",
]
# The Tauri desktop and iOS apps are standalone cargo roots: their platform UI
# dependencies must stay out of this workspace's lockfile and `--workspace` CI.
# See desktop/docs/architecture.md.
exclude = ["desktop/src-tauri", "apps/mobile/src-tauri"]
resolver = "2"

[workspace.package]
version = "0.21.0"
edition = "2021"
license = "MIT"
authors = [
    "James Brink <brink.james@gmail.com>",
    "Jeffrey Dilley <jeff.dilley@gmail.com>",
]
description = "CLI-native local AI image and video generation for people, scripts, and agents"
rust-version = "1.85"
homepage = "https://github.com/utensils/mold"
repository = "https://github.com/utensils/mold"
readme = "README.md"
keywords = ["ai", "image-generation", "diffusion", "flux", "gpu"]
categories = ["command-line-utilities", "multimedia::images", "science"]

[workspace.metadata.crane]
name = "mold"

[profile.release]
lto = "fat"
codegen-units = 1
strip = true

[profile.dev-fast]
inherits = "release"
lto = "thin"
codegen-units = 16
incremental = true
debug = 1
strip = false

# TEMPORARY: local override until candle fork v0.11.1 (restored mode() +
# attention-score bounding) is tagged and published to crates.io.
# Remove this section once 0.11.1 resolves from the registry.
[patch.crates-io]
candle-core-mold = { path = "../candle/candle-core" }
candle-nn-mold = { path = "../candle/candle-nn" }
candle-transformers-mold = { path = "../candle/candle-transformers" }
