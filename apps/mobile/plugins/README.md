# Mold mobile native plugin

Android-only Tauri plugin for platform responsibilities that the shared Vue
mobile app cannot own. It currently provides Keystore-backed per-host API-key
storage. Native discovery, media actions, and appearance synchronization extend
this same bridge instead of creating Android-specific product logic.
