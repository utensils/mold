package com.utensils.mold.mobile_native

import androidx.core.content.FileProvider

/** A distinct provider class keeps Mold's narrowly scoped share cache separate. */
class MoldMediaFileProvider : FileProvider()
