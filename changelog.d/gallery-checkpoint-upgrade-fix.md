- **Servers upgrade cleanly across gallery-record schema growth.** Gallery-authority checkpoints written before #1173 (which added title/favorite/trash fields to `GenerationRecord`) failed their checksum on the re-serialized struct and hard-failed startup on every upgrading install. Validation now falls back to authenticating the stored bytes as written, migrates the checkpoint to the current shape in place through the normal write/backup rotation, and stays immune to future additive record fields; genuinely corrupt checkpoints still hard-fail.
