### Fixed

- Desktop: a finished 3-D print selected from the Now developing rail renders in the Create canvas again. The canvas now loads the GLB over the same native media bridge the Library uses, takes the host's thumbnail as the poster when the completion carried none (so "here's the poster" no longer means a black frame), renews an expired media ticket once before giving up, and re-selecting a print in the rail refreshes a stale result URL instead of only a missing one.
- The video export dialog shows a failed export in the shared error notice, with the copy button, so a long host URL wraps inside the card instead of running off its edge. Error notices everywhere now wrap unbroken URLs and paths.
